{
  "name": "cartelera-zaz-monorepo",
  "private": true,
  "engines": {
    "node": "~14"
  },
  "author": "luism-s",
  "scripts": {
    "setup": "lerna bootstrap --no-ci",
    "setup:ci": "lerna bootstrap --ci",
    "start": "lerna run start --parallel",
    "clean": "lerna run clean --stream",
    "build": "lerna run build --stream",
    "lint": "lerna run lint --stream",
    "test": "lerna run test --stream",
    "heroku-prebuild": "npm run setup:ci"
  },
  "dependencies": {
    "lerna": "4.0.0"
  },
  "keywords": [
    "zaragoza",
    "cinema",
    "movies"
  ]
}
